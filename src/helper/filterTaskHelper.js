// import filterPerDueDate from './dueDateHelper';

const filterTaskHelper = (tasks, filters) => {
  // const dueTasks = { Late: null };
  // const perDue = filterPerDueDate(tasks);
  // console.log('filters-late', filters.Late);
  // if (filters.Late === true) {
  //   const { late } = perDue;
  //   dueTasks.push(late);
  // }
  // console.log('task from filterhelper', dueTasks);
  // return dueTasks;
  console.log(tasks);
  console.log(filters);
};

export default filterTaskHelper;
